/* eslint-disable no-unused-vars */
enum SCENES {
  LOAD = 'sceneLoad',
  MENU = 'sceneMenu',
  GAME = 'sceneGame',
  COLLECTION = 'sceneCollection',
  CREDITS = 'sceneCredits',
}

export default SCENES;
